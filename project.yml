name: Mnemo
options:
  bundleIdPrefix: com.mnemo
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "26.0"
  createIntermediateGroups: true
  groupSortPosition: top

packages:
  MarkdownUI:
    url: https://github.com/gonzalezreal/swift-markdown-ui
    from: "2.0.0"

settings:
  base:
    SWIFT_VERSION: 5.9
    ENABLE_PREVIEWS: YES
    CODE_SIGN_STYLE: Automatic
    CODE_SIGNING_REQUIRED: YES
    CODE_SIGNING_ALLOWED: YES

targets:
  Mnemo:
    type: application
    platform: iOS
    sources:
      - path: Mnemo
        excludes:
          - "**/*.md"
    dependencies:
      - package: MarkdownUI
        product: MarkdownUI
    info:
      path: Mnemo/Info.plist
      properties:
        CFBundleDisplayName: Mnemo
        CFBundleShortVersionString: "0.1.0"
        CFBundleVersion: "1"
        UILaunchScreen:
          UIColorName: ""
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        NSMicrophoneUsageDescription: "Mnemo uses the microphone to capture voice notes for memory extraction."
        NSSpeechRecognitionUsageDescription: "Mnemo uses speech recognition to transcribe voice notes."
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: nest-inc.Mnemo
        INFOPLIST_FILE: Mnemo/Info.plist
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        DEVELOPMENT_TEAM: TX6GM4KP5K
        CODE_SIGN_IDENTITY: "iPhone Developer"
        ENABLE_HARDENED_RUNTIME: NO
        IPHONEOS_DEPLOYMENT_TARGET: "26.0"

  MnemoTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - MnemoTests
    dependencies:
      - target: Mnemo
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.mnemo.tests
        IPHONEOS_DEPLOYMENT_TARGET: "26.0"
        TEST_HOST: $(BUILT_PRODUCTS_DIR)/Mnemo.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/Mnemo

  MnemoUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - MnemoUITests
    dependencies:
      - target: Mnemo
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.mnemo.uitests
        IPHONEOS_DEPLOYMENT_TARGET: "26.0"
        TEST_TARGET_NAME: Mnemo

schemes:
  Mnemo:
    build:
      targets:
        Mnemo: all
        MnemoTests: [test]
        MnemoUITests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - name: MnemoTests
          parallelizable: true
          randomExecutionOrder: true
        - name: MnemoUITests
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
